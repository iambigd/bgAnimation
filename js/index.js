// A $( document ).ready() block.
$( document ).ready(function() {
    console.log( "ready!" );
    backgroundAnimation();
    // $(window).resize(function(){
    // 	backgroundAnimation();
    
    // });
});